# LLM: Web app (frontend)

This folder is a **placeholder**. No framework or code is included. The user adds their chosen frontend (React, Next, Vite, Vue, etc.) here when starting a new project.

Do not assume any FE stack or add boilerplate unless the user explicitly asks for it.
