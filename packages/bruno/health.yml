info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/health"

runtime:
  variables:
    - name: base_url
      value: http://localhost:3001

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
